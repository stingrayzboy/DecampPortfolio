<%= link_to 'New Blog', new_blog_path if logged_in?(:site_admin)%>
<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark covered">
        <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic">A thing of Beauty, joy forever.</h1>
          <p class="lead my-3">&nbsp;</p><br><br>
          <p class="lead mb-0"><a href="#" class="text-white font-weight-bold">&nbsp;</a></p>
        </div>
</div>
<%= paginate @blogs%>

<div class="row">

	<div class="col-md-8">
		<div class="row mb-2">
		
		<%= render @blogs%> 
		<%= paginate @blogs%>
	</div>
	</div>
    <div class="col-md-4">
    	<%= render "shared/blog_sidebar"%>
    </div>
</div>

